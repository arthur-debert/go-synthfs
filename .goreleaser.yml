version: 2

project_name: go-synthfs

builds:
  - id: go-synthfs
    main: ./cmd/go-synthfs
    binary: go-synthfs
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}
    env:
      - CGO_ENABLED=0
    goos: [linux, windows, darwin]
    goarch: [amd64, arm64]
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - id: go-synthfs-archive
    ids: [go-synthfs]
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: "go-synthfs-cli_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - "README.md"
      - "LICENSE"

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "Merge pull request"
      - "Merge branch"

brews:
  - ids: [go-synthfs-archive]
    name: go-synthfs
    repository:
      owner: arthur-debert
      name: homebrew-tools
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    # Use debug directory if DEBUG env var is set, otherwise use Formula
    directory: "{{ if .Env.DEBUG }}debug{{ else }}Formula{{ end }}"
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com
    homepage: "https://github.com/arthur-debert/go-synthfs"
    description: "Description of your CLI tool"
    license: "MIT"
    test: |
      system "#{bin}/go-synthfs --help"
    install: |
      bin.install "go-synthfs"

# nFPM packages (deb, rpm, etc.)
# https://goreleaser.com/customization/nfpm/
nfpms:
  - id: go-synthfs-deb
    ids: [go-synthfs]
    package_name: go-synthfs
    formats:
      - deb
    vendor: "Arthur Debert"
    homepage: "https://github.com/arthur-debert/go-synthfs"
    maintainer: "Arthur Debert <arthur@debert.xyz>"
    description: "Description of your CLI tool"
    license: "MIT"

env:
  - PKG_NAME={{ .Env.PKG_NAME }}
